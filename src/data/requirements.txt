#To install these dependencies, run the following command in your terminal: 
# pip install -r requirements.txt
# use the following command to install the dependencies in a virtual environment:
# python -m venv venv
numpy
pandas
scikit-learn
scikit-image
tqdm
Pillow
fire
medmnist
torch
torchvision
matplotlib
utils
seaborn